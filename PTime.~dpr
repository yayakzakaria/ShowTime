program PTime;

uses
  Forms,
  UTIme in 'UTIme.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.Title := 'Waktu Sekarang';
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
